<% provide(:title, "パスワード変更") %>

    <div class="row">
      <h1>パスワードをお忘れの場合</h1>

        <div class="mb-3">
          <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
            <%= render "users/shared/error_messages", resource: resource %>
        </div>

        <div class="my-3">
          <h2>登録したメールアドレスを入力して、送信ボタンを押してください。<br> 登録メールにパスワードを送信します。</h2>
        </div>

      <div class="margin-left">
              <div class="mb-4">
                <%= f.label :email, class:'form-label' %>
                <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control w-75' %>
              </div>

              <div class="mb-3">
                <%= f.submit "送信", class: "btn btn-success w-75", style: 'background-color: rgba(91, 33, 0, 0.97);border-color: rgba(91, 33, 0, 0.97);' %>
              </div>
          <% end %>

      <%= render "users/shared/links" %>
  </div>
<div>
